# Custom Nginx Config für bessere Performance
client_max_body_size 100M;
proxy_connect_timeout 60s;
proxy_send_timeout 60s;
proxy_read_timeout 60s;

# Rate Limiting
limit_req_zone $binary_remote_addr zone=api:10m rate=10r/m;