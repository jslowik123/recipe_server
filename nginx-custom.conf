# Security headers and file protection
location ~* \.(env|git|config|bak|backup|yml|yaml|json|log|ini|conf|DS_Store)$ {
    deny all;
    return 403;
}

location = /robots.txt {
    return 204;
}

location = /favicon.ico {
    return 204;
}